'use strict';

var getJson = require('./getSlackContent.js');



var getContentFromSlackChannel = function (messages, callback) {

  // there are two i care about "Phase: Started", "Phase: Completed"

  if (callback) {
    // going to return status based on the json phase


  }
}
